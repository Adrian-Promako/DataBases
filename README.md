! [image](<Ejemplos%20de%20SQL/22-11-45-landing-mysql-banner-img.jpg>)

__…or create a new repository on the command line__  
```
echo "# DataBases" >> README.md  
git init  
git add README.md  
git commit -m "first commit"  
git branch -M main  
git remote add origin https://github.com/Adrian-Promako/DataBases.git  
git push -u origin main
``` 
  
__…or push an existing repository from the command line__  
```
git remote add origin https://github.com/Adrian-Promako/DataBases.git  
git branch -M main  
git push -u origin main
```
